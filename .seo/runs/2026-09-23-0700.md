# Daily SEO — September 23, 2026 (Istanbul)

## Choice

Repair public error rendering. Live GET and Chromium probes found HTTP 500 on
`/blog`, `/docs/api-reference/mcp`, and a unique nonexistent URL, although the
canonical pages were reachable. Production logs showed closed database
connections during error-page context processing. Reachability takes priority
over new editorial content, discovery nudges, and analytics enhancements.

## Change and verification

A dedicated 404 handler renders without request context processors. The error
page keeps its recovery links and light/dark styling, but does not load marketing
context, application JavaScript, analytics, or product schema. Django's existing
slash-redirect policy and query strings are preserved; no new redirect rules.

- Twelve GET/HEAD regression cases fail before and pass after the repair with
  database cursors unavailable; the error path makes no database queries.
- All 155 page tests pass; Django system and migration checks pass.
- Chromium verifies 301-to-200 blog/MCP aliases, query-string preservation, and
  real 404/noindex responses. Mobile/desktop and light/dark screenshots inspected;
  no horizontal overflow, one H1, keyboard-accessible recovery link, no analytics.
- Repository CI, merge, deployment, and live recheck are pending at PR creation.
  Actual lifecycle and exact evidence are maintained in the existing private
  Rowset run `2026-09-23-0700`; this file does not claim deployment.

## Continuity and limits

PR #28 was already merged by Rasul at
`75d65cc98fbbef569f7484e132869a7aeccf40d2`; do not recreate that launch. Search
Console confirms all four starter articles indexed with matching canonicals.
Continue observing the fixed cohort before expanding it. The blog hub and MCP
guide are still unknown to Google. Search samples remain too small to infer
performance. Public docs/blog attribution uses route templates; conversions and
field Core Web Vitals are not established.

The existing homepage truth rule still expects retired wording. This is a stale
check, not evidence of a live false claim; revalidate it in a later run. Existing
intentional authentication redirects and the low-priority `/uses` orphan remain.
No new paid research calls ($0 incremental research); reuse the September 20
research. No outreach, social posts, tracker, or schedule changes. No human
decision is needed.

## Review authority

ReviewGate is explicitly **waived by Rasul**, not passed, for this SEO repair:
September 20, 2026, Slack #tastefulkit message `1789910859.518629`, thread
`1789907027.853529`, reiterated in this scheduled task. The PR body and private
run record bind this waiver to the exact PR head SHA. CI, tests, branch/PR/merge
workflow, and live verification remain required. Repository protections and the
ReviewGate service are unchanged.
